'use client'
import { Navigation,MapPin } from 'lucide-react'
import type { Post } from './Feed'
function km(aLat:number,aLon:number,bLat:number,bLon:number){const R=6371,dLat=(bLat-aLat)*Math.PI/180,dLon=(bLon-aLon)*Math.PI/180;const x=Math.sin(dLat/2)**2+Math.cos(aLat*Math.PI/180)*Math.cos(bLat*Math.PI/180)*Math.sin(dLon/2)**2;return 2*R*Math.asin(Math.sqrt(x))}
export default function NearbyMap({posts,userLocation}:{posts:Post[];userLocation:{latitude:number;longitude:number}|null}){if(!userLocation)return <div className="map-permission"><div className="map-permission-icon"><MapPin size={28}/></div><h3>הפעל מיקום כדי לראות מה קורה סביבך</h3><p>המפה תציג רק עדכונים עם מיקום ששותף מרצון.</p></div>;const withDistance=posts.filter(p=>p.latitude!=null&&p.longitude!=null).map(p=>({...p,distance:km(userLocation.latitude,userLocation.longitude,p.latitude!,p.longitude!)})).sort((a,b)=>a.distance-b.distance).slice(0,20);const d=.025;const bbox=`${userLocation.longitude-d},${userLocation.latitude-d},${userLocation.longitude+d},${userLocation.latitude+d}`;const src=`https://www.openstreetmap.org/export/embed.html?bbox=${encodeURIComponent(bbox)}&layer=mapnik&marker=${userLocation.latitude}%2C${userLocation.longitude}`;return <div className="nearby-wrap"><div className="osm-card"><iframe title="מפת OpenStreetMap" className="osm-frame" src={src}/><div className="map-live-badge">● LIVE · {withDistance.length} עדכונים עם מיקום</div></div><div className="map-legend"><span><i className="legend-dot me"/>המיקום שלי</span><span><i className="legend-dot status"/>עדכון פעיל</span></div><div className="nearby-list">{withDistance.length?withDistance.map(p=><div className="nearby-item" key={p.id}><div className="nearby-pin"><MapPin size={17}/></div><div className="nearby-copy">{p.location_name&&<strong>{p.location_name}</strong>}<p>{p.text}</p><span>{p.distance<1?`${Math.round(p.distance*1000)} מטר`:`${p.distance.toFixed(1)} ק״מ`} ממך</span></div><a className="navigate-btn" target="_blank" rel="noreferrer" href={`https://www.openstreetmap.org/?mlat=${p.latitude}&mlon=${p.longitude}#map=17/${p.latitude}/${p.longitude}`}><Navigation size={16}/></a></div>):<div className="empty small-empty">אין כרגע עדכונים עם GPS באזור שלך.</div>}</div></div>}
